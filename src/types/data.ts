export interface IUser {
    name?: string | null,
    surname?: string | null,
    phone?: string | null,
    email?: string | null,

}

export interface IHeader {
    title: string;
    description: string
}